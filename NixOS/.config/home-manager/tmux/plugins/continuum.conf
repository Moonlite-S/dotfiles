# tmux-continuum: automatically restore the last tmux-resurrect save
# when the tmux server starts (e.g. after a reboot). Leaves the
# existing ~15min autosave interval at continuum's default - no
# @continuum-save-interval override.
set -g @continuum-restore 'on'
