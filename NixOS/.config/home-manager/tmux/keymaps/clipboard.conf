# Let OSC 52 clipboard-set sequences from apps running inside tmux
# (nvim, etc.) pass through to the real system clipboard, and let
# tmux's own copy-mode do the same when you yank there. Depends on
# wl-clipboard being installed (home.nix) for anything to actually
# read/write the Wayland clipboard underneath this.
set -g set-clipboard on
