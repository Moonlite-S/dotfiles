-- mini.jump: enhances the native f/F/t/T/;/, character-search
-- motions in place (same keys, no new mappings) - repeatable with
-- bare `;`/`,` after moving off the line, briefly highlights the
-- matched character, and can search across multiple lines.
require('mini.jump').setup({})
