-- mini.bracketed: adds many [x/]x next/previous navigation pairs
-- (buffer, comment, conflict, diagnostic, file, indent, jump,
-- location-list, oldfile, quickfix, treesitter, undo, window, yank) -
-- like vim-unimpaired. Discoverable for free: `[`/`]` are already
-- mini.clue triggers, so these show up in that popup too.
require('mini.bracketed').setup({})
