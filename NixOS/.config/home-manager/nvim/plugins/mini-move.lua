-- mini.move: move the current line, or a visual selection, in any
-- direction. Defaults are Alt+hjkl in both Normal (moves the line)
-- and Visual (moves the selection) mode - checked, nothing else in
-- this config uses <M-h/j/k/l>.
require('mini.move').setup({})
